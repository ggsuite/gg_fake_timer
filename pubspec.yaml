name: gg_fake_timer
description: A class faking dart's Timer class. Can be used for test scenarios.
version: 1.0.5
repository: https://github.com/inlavigo/gg_fake_timer

environment:
  sdk: ">=3.13.0 <4.0.0"

# Add regular dependencies here.
dev_dependencies:
  args: ^2.7.0
  gg_capture_print: ^1.1.0
  lints: ^6.1.0
  test: ^1.31.2